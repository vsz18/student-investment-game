services:
  - type: web
    name: student-investment-game
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production

# Made with Bob
